{
  "schema": "schema.graphql",
  "documents": ["frontend/**/*.graphql", "graphql/tests/**/*.graphql"],
  "extensions": {
    "endpoints": {
      "default": {
        "url": "http://localhost:4000/graphql",
        "subscription": {
          "url": "ws://localhost:4000/graphql"
        }
      }
    }
  }
}
